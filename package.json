{
  "name": "esayemm-styles",
  "version": "1.0.2",
  "description": "Generic styles",
  "scripts": {
    "version": "git add .",
    "postversion": "git push && git push --tags && npm publish",
    "deploy": "./node_modules/kontinuum-deploy/bin/kontinuum_deploy --input ./deploy/esayemm-styleguide.yaml.tpl --output ./deploy/esayemm-styleguide.yaml --service esayemm-styleguide"
  },
  "author": "Sam Lau",
  "license": "ISC",
  "devDependencies": {
    "kontinuum-deploy": "^1.1.2"
  },
  "main": "./index.scss"
}
